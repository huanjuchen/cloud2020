server:
  port: 9003

spring:
  application:
    name: payment-provider-nacos
  cloud:
    nacos:
      discovery:
        server-addr: 127.0.0.1:8848 #nacos地址

#监控配置
management:
  endpoints:
    web:
      exposure:
        include: '*'